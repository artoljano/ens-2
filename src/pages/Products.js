import React from 'react';

import {Hero, Header, Catalog} from '../shopSections'

const Products = () => (
  <div>

    <Hero />
    <Header />
    <Catalog />

  </div>
);

export default Products;
